import { APPLICATION_MODULE } from './application/application.registry';
import { INFRA_REGISTRY } from './infra/infra.registry';

export const MODULE = {
  INFRA: INFRA_REGISTRY,
  APPLICATION: APPLICATION_MODULE,
};
